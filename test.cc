#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main()
{
    vector<int> res;
    vector<int> res1;
    res.push_back(1);
    res.push_back(2);
    int *a;
    a = new int(10);

//    res1 = res + 1;

    //std::find(res.begin(), res.end(), 1)!=res.end();
}
